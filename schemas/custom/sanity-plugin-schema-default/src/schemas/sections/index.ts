/*
 *  Add sections with custom changes here that you want to reflect on current project.
 */

import { default as header } from "./header/header";

const schemas = {
  header,
};

export default schemas;
